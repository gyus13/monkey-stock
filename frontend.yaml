apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: monkey-ingress-alb
  labels:
    service: frontend
    project: monkeystock
spec:
  replicas: 2
  selector:
    matchLabels:
      service: frontend
      project: monkeystock
  template:
    metadata:
      labels:
        service: frontend
        project: monkeystock
    spec:
      containers:
      - name: frontend
        image: dev210/monkey-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        
---
apiVersion: v1
kind: Service
metadata:
  name: alb-service
  namespace: monkey-ingress-alb
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: NodePort
  selector:
    service: frontend